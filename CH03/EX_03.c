#include <stdio.h>

int main(void)
{
    /*
    a) "%d" vs " %d"
    - same
    b) "%d-%d-%d" versus %d -%d -%d
    different second allows optional whitespace
    c) "%f" versus "%f "
    different waits on a whitespace in the second
    d)"%f,%f" versus "%f, %f"
    same
    */
    return 0;
}