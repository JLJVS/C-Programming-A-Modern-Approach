#include <stdio.h>

int main(void)
{

    // sizeof(a) gives the total size of the array a, if you now use sizeof(t) instead of sizeof(a[0]) you have to initalize an additional type and are more prone to making a mistake.
    return 0;
}